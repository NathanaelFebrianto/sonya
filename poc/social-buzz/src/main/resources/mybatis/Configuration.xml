<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<properties resource="mybatis/jdbc.properties" />

	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<mapper resource="mybatis/mapper/PostMapper.xml" />
		<mapper resource="mybatis/mapper/CommentMapper.xml" />
		<mapper resource="mybatis/mapper/MetooMapper.xml" />
		<mapper resource="mybatis/mapper/TvProgramMapper.xml" />
		<mapper resource="mybatis/mapper/TermMapper.xml" />
		<mapper resource="mybatis/mapper/TweetMapper.xml" />
	</mappers>
</configuration>